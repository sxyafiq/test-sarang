import React from "react";

interface MuaPullProps {
  category: string;
  title: string;
  user?: string;
}

const MUA = ({ category, title, user }: MuaPullProps) => {
  return <div>MUA</div>;
};

export default MUA;
